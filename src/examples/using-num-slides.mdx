---
name: with Slides
---

# Example with multiple Slides

import {Fragment, useState} from 'react'
import { Playground, Props } from 'docz'
import ReactSlidy from '../index.js'
import Number from '../examples/Number'
import '../index.scss'

## multiple slides

Use `numOfSlides` prop to determine the number of slides.

<Playground>
  <ReactSlidy numOfSlides={3}>
    <Number num={1} />
    <Number num={2} />
    <Number num={3} />
    <Number num={4} />
    <Number num={5} />
    <Number num={6} />
    <Number num={7} />
    <Number num={8} />
  </ReactSlidy>
</Playground>

## multiple slides when numOfSlides is greater than length

<Playground>
  <ReactSlidy numOfSlides={5}>
    <Number num={1} />
    <Number num={2} />
    <Number num={3} />
  </ReactSlidy>
</Playground>

## with a single slides when numOfSlides is greater than 1

<Playground>
  <ReactSlidy numOfSlides={5}>
    <Number num={1} />
  </ReactSlidy>
</Playground>